function setup() {
	createCanvas(1000, 1000);
};

function draw() {
	background(0, 0, 0);
	fill(0, 255, 255);
	textSize(30);
	text("Hello Class", 100, 100);
};

// Class used sublime text and browser sync. Here I used VSCode and Live Server Extension
// Steps (Roughly) Below:
// Ctrl + Shift + P
// Search "Package Control: Install Package"
// Look up "Browser Sync"
// Click on Browser Sync